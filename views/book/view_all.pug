extends ../layout

block content
    h3 Books
    a(href='/books/add') Add Book
    table
        thead
            tr
                th Book Title
                th Genre
                th
        tbody
            each book in books
                tr
                    td= book.title
                    td= book.genre
                    td
                        a(href=`/books/profile/${book.id}`) View Book